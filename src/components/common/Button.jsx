const Button = ({btnName}) => {
    return (
        <button className="px-5 py-2 text-center cursor-pointer rounded-xs bg-primary-200 text-white ">{btnName}</button>
    );
};

export default Button;